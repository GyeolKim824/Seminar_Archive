202301558 김결

// 문제 2
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int print(int n) {

	if (n == 0) {
		return 0;
	}
	else {
		print(n / 10);
		printf("%d\n", n % 10);
	}
}

int main() {
	unsigned int N;
	scanf("%d", &N);
	print(N);
}


// 문제 3
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int print(int n) {

	if (n == 0) {
		return 0;
	}
	else {
		printf("%d\n", n % 10);
		return print(n / 10);
	}
}

int main() {
	unsigned int N;
	scanf("%d", &N);
	print(N);
}

// 문제 4
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int str[20];

int maximum(int index) {

	int temp;

	if (index == 0) {
		return str[0];
	}
	temp = maximum(index - 1);
	if (temp < str[index]) {
		return str[index];
	}
	else {
		return temp;
	}

}

int main() {

	int N;
	scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		scanf("%d", &str[i]);
	}
	printf("%d", maximum(N-1));

}

// 문제 5
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

void hanoi(int n, char from, char tmp, char to) {
	if (n == 1) {
		printf("%c %c\n", from, to);
	}
	else {
		hanoi(n - 1, from, to, tmp);
		printf("%c %c\n", from, to);
		hanoi(n - 1, tmp, from, to);
	}
}

int main(void) {
	char A='A';
	char B = 'B';
	char C = 'C';
	int N;
	scanf("%d", &N);
	hanoi(N, A, B, C);
	return 0;
}

// 문제 6
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int gcd(int a, int b){
	if ((a % b) == 0) {
		return b;
	}
	else {
		return gcd(b, a % b);
	}
}

int main() {
	int i, j, k;
	scanf("%d %d", &i, &j);
	if (i > j) {
		k=gcd(i, j);
	}
	else {
		k = gcd(j, i);
	}
	printf("%d", k);
}